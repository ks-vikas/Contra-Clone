using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.UI;

/*public class Gun : MonoBehaviour
{
    [SerializeField] GameObject player;
    
    [SerializeField] private Image[] Guns;
    private int gunNumber;
    
    
    

    private void Start()
    {
        gunNumber = player.GetComponent<CurrentGun>().playerGun;
        Guns[gunNumber].fillAmount = 1;
        Debug.Log(gunNumber);
    }
    private void Update()
    {
        health = player.GetComponent<PlayerLife>().playerHealth;
        currenthealthBar.fillAmount = health / 100;
    }
}*/